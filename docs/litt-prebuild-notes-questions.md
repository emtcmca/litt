Are we tasking the coordinator with too much? Worthwhile to develop and implement additional agents to take the full load off the coordinator?  Thinking about an air traffic controller agent, tasked only with routing workflow initially, for example.

Persisted memory/data storage. How are we addressing this in a way that is legal grade, enterprise level, redundant(?), bulletproof? 

How are we handling sensitive data and ensuring foolproof segregation of data (per matter, per client, per attorney, etc.)?

Do we need to build oracles tasked with reviewing coordinator output or the output of any agent(s) at any point in the operation of the agentic workflow?